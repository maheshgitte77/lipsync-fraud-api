fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# MediaPipe lip–audio correlation (same env as uvicorn):
# OpenCV comes from syncnet_python/requirements.txt (opencv-contrib-python).
mediapipe>=0.10.0
librosa>=0.10.0
soundfile>=0.12.0

# SyncNet pipeline (run in the same environment as uvicorn):
#   pip install -r syncnet_python/requirements.txt

# Install PyTorch separately for your platform, e.g. CPU (Linux EC2):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
