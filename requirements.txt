# --- Web framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic>=2.5.0

# --- Computer vision / audio ---
mediapipe>=0.10.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
# OpenCV is provided by syncnet_python/requirements.txt (opencv-contrib-python)

# --- Async / streaming ---
kafka-python>=2.0.2
redis>=5.0.0
scenedetect>=0.6.4
python_speech_features>=0.6

# --- Optional: cloud storage ---
# boto3>=1.34.0

# --- Optional: TTS providers ---
# google-cloud-texttospeech>=2.16.0
# (ElevenLabs, Deepgram, Cartesia, Inworld use plain HTTP via stdlib — no SDK required.)

# --- PyTorch (install separately for correct CUDA/CPU wheels) ---
# CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Avatar generation is HeyGen API based (no local Wav2Lip/SadTalker model deps).
